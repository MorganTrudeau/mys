import React from "react";
import { Grid } from "@material-ui/core";
import styles from "./BillingMain.module.css";

const BillingMain = props => {
  return (
    <Grid
      container
      justify={"center"}
      alignItems={"flex-start"}
      className={styles.container}
    >
    </Grid>
  );
};

export default BillingMain;
